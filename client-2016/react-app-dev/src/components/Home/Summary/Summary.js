import React from 'react';
import Panel from 'react-bootstrap/lib/Panel';
import Brief from '../Brief/Brief';
import text from '@styles/text.css';

const Summary = () => (
  <div className={`${text.center} ${text.regular}`}>
    <p>Experience in big data real-time analytics, many different kinds of applications from banking to e-commerce in several programming languages and frameworks across the full stack and software life cycle, with an understanding of the need to balance time constraints with careful design and best practices.</p>
  </div>
);

export default Summary;
